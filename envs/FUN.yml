name: fun_anno_env
channels:
  - conda-forge
  - defaults
  - bioconda
dependencies:
  - python=3.11
  - eggnog-mapper
  - ruamel.yaml
  - httpx
  - psutil
  # need this version of python to work
  - pip:
    - kegganog

